{
  "packages": ["packages/*"],
  "version": "independent",
  "command": {
    "version": {
      "ignoreChanges": ["**/__tests__/**", "**/*.md", "**/*.spec.*"],
      "message": "Release"
    }
  }
}
